# 3.1.9-Beta

#### &#x20;New Feature

1\. On Demand Access Request Confluence 1.5.2.0 1.5.2.0 access dynamically, Document Link:- In Progress (Bugs Reported) 3.1.9 Google Docs Cymmetri should offer the capability to define on-der.&#x20;

2\. Recommendation Engine -Rejected Due to Major bug present a. Recommendation Engine Campaign: Recommendation on assignment approver action in campaign for campaign assign. b. Recommendation Engine Inbox: Recommendation on application assignment workflow request in inbox for approver&#x20;

3\. Multiple Reports Added from Mahindra Finance IGA Reports. List will be shared separately (In Insights > Reports) section. -- Not Done (Understanding information not received)&#x20;

4\. Workflow enhancement, if workflow-task assignee is same as target-user/requester then assign task to assignee's reporting-manager. Application level configuration support provided, default is disabled.&#x20;

5\. Advanced Analytics > Authentication Data model provided. Not Done (Understanding information not received)&#x20;

6\. UI/UX-Role description in request application flow - Done 1 bug raised&#x20;

7\. List of User to be deprovision as per Deprovision Rule:- This feature is to provide visibility into which users are selected for deprovisioning based on specific deprovision rules, such as End Date and Status. Currently, users lack the ability to see which individuals are flagged for deprovisioning according to these criteria. By implementing this feature, users will gain insight into which users are being targeted for deprovisioning as per the configured rules, enabling better tracking and planning. Not Tested as this feature was not made for client, was added for checking internally Document Link:- Google Docs&#x20;

8\. On Demand Access Request Extend&#x20;

&#x20;   a. Global App-Done (4 Minor test cases reopened)

#### Fixes

1. Provision Rule-While adding the condition as Group getting invalid argument&#x20;
2. Campaign Notification In Create campaign template body showing incorrect spelling of started, it is showing "stared" &#x20;
3. Application delete Application delete with having workflow list present, able to reassign worldlow&#x20;
4. Bulk UnAssignment-Showing error message when import history > record is clicked &#x20;
5. Application provisioning and deprovisioning workflow initiated request is not getting displayed in activity logs.&#x20;
6. Farm Lagic || Preview form resulting in blank page (crashing)&#x20;
7. Recon History- User activity logs UI for Pull and Push recon are not same &#x20;
8. &#x20;Advanced Analytics-Not able to disable scheduled email report, even after saved dashlets is disabled&#x20;
9. Tooltip Glitch || Infinite open &#x20;
10. External Logs || Page blank when loading &#x20;
11. External log To date filter on date selection current time should be default selected, currently taking by default 12am&#x20;
12. Quick setup Amaya-Validation message should be changed&#x20;
13. Application-Roles Update Status Change the success message of status update from active/inactive&#x20;
14. Advanced Analytics-Cron expression not updated as per user configured&#x20;
15. Roles based form-When the form schema is not loaded (Load sample data is not clicked) and an application with a role is assigned to a &#x20;
16. Reconciliation Dashboard || Redirect to history-Back not working &#x20;
17. Provision Rule-While adding the condition as Group getting invalid argument&#x20;
18. Campaign Notification In Create campaign template body showing incorrect spelling of started, it is showing "stared"&#x20;
19. Campaign-Notification/scheduling Need to change the Labels&#x20;
20. UI Distortion || Chrome 130 || All layout pages breaking&#x20;
21. Skip Password Expiry-System is not saving the configuration while enabling the checkbox.&#x20;
22. &#x20;Provision Rule-Application Roles are not getting updated as per rule after updating user details&#x20;
23. &#x20;UI/UX || Remove redundant library (lottie)&#x20;
24. Password expiry is getting expired tomorrow, but message showing "Your password will expire today "
25. If tenant is not working, Change the error message "AUTHSRVC.UNAUTHORIZE" to Unauthorize
26. Delegator user login - If delegator user is inactive, then delegatee tries to login, getting error "Delegated user not active"&#x20;
27. Delegator user login - If delegator user is deleted, then delegatee tries to login, getting error "Delegated user not active"
28. Recon notification On abort, email received multiple time
29. Advanced Analytics - Cron expression not updated as per user configured &#x20;
30. Advanced Analytics-Scheduled report emails are still being triggered even after the saved Dashlet has been deleted.&#x20;
31. Advanced Analytics-Not able to disable scheduled email report, even after saved dashlets is disabled&#x20;
32. Form Logic - Showing blank page on preview while removing the required array from the json template
33. On-demand Access-Audit log for enabling/disabling toggle is not visible&#x20;
34. On-Demand Access- Showing All applications in My Request> On demand access even when condition is not matching
35. On-Demand Access- In URL showing self service designer, which is not present in lifecycle management&#x20;
36. Recon notification-On abort, email received multiple time&#x20;
37. Inbox-Recommendation icon should be in the green color for the recommended&#x20;
38. Recommendation-Inbox, Recommendation present then also showing message recommendation not found

#### Known Bugs

1. manager notification: receiving user name required manager name&#x20;
2. In application setting if show to user flag off then also application show in recent application.&#x20;
3. Amaya || Unable to identify application properties data type where value is empty&#x20;
4. Amaya || Create user operation fails due to invalid password 5. 6. 7.&#x20;
5. Reports- Records are getting displayed after 11min approximately&#x20;
6. Deprovision Rule executed via Scheduler - Workflow is not getting initiated for the set of users on the basis of status/end '
7. date Workflow List - Getting error" Contact system administrator on technova tenant&#x20;
8. FormLogic || Step 2 of form || Form config JSON should be mandatory



\






