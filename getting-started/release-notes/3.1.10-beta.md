# 3.1.10-Beta

#### &#x20;New Feature

&#x20;1\. Role Management QA Done (1 Minor TC reopened)&#x20;

&#x20;   a. CSV Import supports for parent and child role&#x20;

&#x20;   b. Acided Parent Rolle and Child Role fields when creating a role manually.&#x20;

2. Advanced Analytics: provided user the option to download the report either as csv or pdf Not Done (On hold)&#x20;
3. Save as Draft feature in approvers view in active campaign-QA done&#x20;
4. Initial planned end date in tooltip after the campaign gets extended&#x20;
5. ScriptOn(Database) Connector:--- QA Done a. Manual Link operation support added

#### Fixes

1. Campaign - Campaign Summary Report - Received email showing no subject and body details are not proper, not as per shared in email&#x20;
2. Campaign-Pending/History - Email Report - No error message is coming if selected email address is more than 20 count &#x20;
3. Campaign - Notifications/scheduler - If total count of user selection in email address in Enable Campaign Email Reports and Campaign Completion Notification is more, needs to change error messsage&#x20;
4. Campaign-Download Report - While opening the csv file in excel,data in stage column showing in date format.&#x20;
5. On Demand-Audit log-Add config in audit log description&#x20;
6. UI Glitch || Notification content overflowing&#x20;
7. Provision Rule- UI for roles is not proper&#x20;
8. Import Mapping-Ul for Remap button is not correct&#x20;
9. Self Service Roles Based Form-Ul of Role Form is not correct visible in view form when role based form application is assigned to user at the time of application assignment&#x20;
10. Inbox-Workflow Request for setup for application - Need to enhance the details as per user interface.&#x20;
11. Form logic-When "required" property removed from json then, form preview not working&#x20;
12. On Demand Access-Search button should be present on self service side&#x20;
13. When adding any application if you double click really fast 2 applications with same name but different appid gets added&#x20;
14. Reports- Apply Filter' checkbox remains checked even after unchecking it and saving the configuration
15. Internal Authentication-On Disabling the Cymmetri auth toggle, and when trying to save the configuration, it shows loading&#x20;
16. Bulk Application Assignment - Workflow is getting initiated if workflow is configured for that application while assigning application in bulk&#x20;
17. Self approval Workflow - If application has workflow configured then workflow is getting initiated for the user even though that comes in self approval condition&#x20;
18. On-Demand Access- Showing error message when enabling toggle&#x20;
19. User Threshold Config - Details are getting saved without "Send threshold notifications to as this field is mandatory
20. Recommendation calculation-Deleted user should not be consider for the calculation part&#x20;
21. Recommendation-Under campaign recommendation not showing, Previously it was showing, same time for inbox it is showing&#x20;
22. Inbox-Recommendation showing null&#x20;
23. Users-Application Assignment-While assigning an application with role, searched role is not visible while entering role name.&#x20;
24. Global apps config-Ul is not proper when long name role is selected&#x20;
25. recommendation\_engine\_job\_history- scheduler Job not getting end, multiple running statuses present&#x20;
26. Import/Export application- Ternant credentials are getting saved after importing application&#x20;
27. Role Management- Parent role id of one application is getting linked with different application role ids&#x20;
28. versions-cisodashboardsrvc showing error when connected without VPN &#x20;
29. Users-Application Assignment-While assigning an application with role, searched role is not visible while entering role name.&#x20;
30. Role Description: Role Description not visible for roles shown while Assigning Roles from My Workspace&#x20;
31. Role Import-Role Hierarchy page > Zoom(+/-) button should be fixed&#x20;
32. Roles Import-Child attributes are getting overlapped&#x20;
33. On-demand> Add Role-Parent child fields are not available in on demand configuration&#x20;
34. passwordfilter-after updating the password from cymmetri -forgotpassword flow, then getting write conflict&#x20;
35. My Access Self service - Inactive roles are visible in user account while requesting roles while assigning application and role&#x20;
36. On Demand Access-Configuration without application should not be saved&#x20;
37. Application admin> Global apps config-Page should not be visible to applictaion admin also should not be able to save configuration&#x20;
38. Global Apps Access-Admin user is able to save empty applications field configuration&#x20;
39. Read Only admin-Showing processing please wait message when clicked on On-demand access rule (showing two field do not match)&#x20;
40. Global Apps - 2 validation message are shown when empty application field is saved&#x20;
41. OnDemand Rule- Page is getting unresponsive( hanged) when clicked on Add application multiple times&#x20;
42. On Demand Access- The 'Assign Role' option should not be visible in the application's menu actions if a role has already been assigned via On-Demand and no new roles are available to request .
43. Report Application Admin> Global apps config-Page should not be visible to report app admin also should not be able to save config&#x20;
44. Global Apps- User is not able to assign role after application is successfully assigned to user&#x20;
45. Global Apps-Target user is not able to request updated role

#### Known Bugs

1. &#x20;manager notification: receiving user name required manager name&#x20;
2. In application setting if show to user flag off then also application show in recent application.&#x20;
3. Amaya || Unable to identify application properties data type where value is empty&#x20;
4. Amaya || Create user operation fails due to invalid password&#x20;
5. Reports-Records are getting displayed after 11min approximately&#x20;
6. recommendation run for tenant 2711 taken 2 days,3 hours, 8 minutes, and 12 seconds for 345,000 users

