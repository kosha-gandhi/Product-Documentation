# 3.1.15 -Beta

#### New Features

1. Access Review: Application with role scope filter&#x20;
2. Workflow Rule Evaluator: Using Workflow Rule Evaluator administrators can find all matched workflow rules base on selected workflow event and attributes.&#x20;
3. Time-based application role assignment and un-assignment support for multiple role.&#x20;
4. &#x20;Introduced generator with rule engine so based on rules generator will be executed.&#x20;
5. Mover trigger attribute evaluation changed from to . (For AND, all configured attributes matching was required. Now if any configured trigger attribute is matched then mover process will be initiated)&#x20;

#### Fixes

1. My Access- Remove search by tags watermark from search field
2. Timebase-Selfservice - While clicking on timebase view icon, it is redirecting sso page&#x20;
3. Timebase Selfservice Exceptional access getting error while assigning the application &#x20;
4. Timebase My access While assigning role via selfservice, it is redirecting to exceptional access Discussion required&#x20;
5. Mover with OR- Deproision call getting repeated with success

#### Known Bugs

1. manager notification: receiving user name required manager name &#x20;
2. In application setting if show to user flag off then also application show in recent application &#x20;
3. Amaya || Unable to identify application properties data type where value is empty &#x20;
4. Amaya || Create user operation falls due to invalid password&#x20;
5. Reports-Records are getting displayed after 11min approximately &#x20;
6. recommendation run for tenant 2711 taken 2 days, 3 hours, 8 minutes, and 12 seconds for 345,000 users
7. During reconciliation, when the user login ID is left blank and the loginGenerator is activated, the process fails due to the empty login

