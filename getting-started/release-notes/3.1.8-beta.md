# 3.1.8 -Beta

#### New Feature

1. \[Experimental] External Logs - This feature provides a centralised way to capture and view logs from external applications interacting with Cymmetri via webhooks or batch tasks. This enables administrators to debug external interactions directly within Cymmetri, simplifying the monitoring and troubleshooting process.&#x20;
2. &#x20;Reconciliation Activity Log-Show previous and next 10 minutes activity log for each reconciled entity (user/group)
3. &#x20;Reports Email Scheduling - Configuration support added for fetching report of current business day.
4. &#x20;Campaign Notifications for Managers, Internal users and External Emails. \\
5. &#x20;Campaign Report downloads feature.&#x20;
6. &#x20;Extend Running Campaign.&#x20;
7. &#x20;Campaign reviewer/manager/admin can see the last login for access review request.&#x20;
8. &#x20;Scheduler optimisations, execute scheduled task for active tenants only-QA Done&#x20;
9. Recon Dashboard Enhancement:&#x20;

&#x20;      a. Remove global-level recon notification configuration.&#x20;

&#x20;      b. Provide application-level recon notification configuration.

&#x20;      c. Added a new recon failure notification template.&#x20;

10. Provide an unboundid group API to add or remove members from a group in Active Directory that has been added to the product. 11. Bulk application un-assignment.



#### Fixes

1\. Refactor lodash imports to reduce bundle size

&#x20;2\. Web URL- Link is not visible in web url when dicked on Products module

&#x20;3\. Simple AD-Group is getting updated after unlink operation is executed from recon pull&#x20;

4\. Threshold configuration-Mails should not be triggered to inactive users&#x20;

5\. Custom Attribute - Old records are not getting displayed in workflows, provision rule while selecting condition as custom attribute&#x20;

6\. Custom Attribute If custom attribute is created as Group, it is getting displayed in workflow and provision dropdown&#x20;

7\. Workllow List/Pending Workflow-Created At-Value showing Updated date when approver approves/rejects the request.&#x20;

8\. User delete workflow-Workflow List-User detail showing error

&#x20;9\. Global workflow toggle enable showing error, in the server log showing write conflict, some time occurs&#x20;

10\. Scheduler- For tenant whose subscription has end, schedulers/notification should be stopped and after resuming subscription process should be resumed as well.&#x20;

11\. AD-Recon-If Policy attribute marked inactive then also it is getting pull from AD&#x20;

12\. AD new bundle-When recon is executed for both exist update but application is not linked, still user is getting updated.&#x20;

13\. Simple AD- Group is getting updated after unlink operation is executed from recon pull&#x20;

14\. Recon Failure sync status-Showing "Reconciliation In Progress" in fail status&#x20;

15\. Application admin domain> Dashboard recon- Showing processing please wait when clicked on Logs&#x20;

16\. Audit Recon- Audit log for recon completion is not showing&#x20;

17\. Recon-Abort sync state-System is getting auto abort and failure log showing nothing

18.Recon History- User activity logs Ul for Pull and Push recon are not same&#x20;

19\. Application admin- Should be able to edit/save notification users&#x20;

20\. Read only admin-Should not be able to abort recon, read only should be able to only see it&#x20;

21\. Recon Dashboard- Data in the Application Name column is mismatched between the reconciliation dashboard and the application reconciliation for the reconciliation abort failure log&#x20;

22\. Recon dashboard-Recon type(Pull/Push) column should be added in dashboard page&#x20;

23\. External log-Autorefresh not working, need to scroll down manually&#x20;

24\. Read only user- Showing processing please wait when dicked on Logs&#x20;

25\. Recon Dashboard-When aborting the reconciliation process, the dashboard shows the status as "Finished," but the application reconciliation history shows the status as 'Aborted&#x20;

26\. Application-Recon history -outer and inner status mismatch&#x20;

27\. Recon aborted Log- When recon is aborted, audit log should be changed&#x20;

28\. Recon Dashboard/ History-Showing recon status In progress even when recon is completed&#x20;

29\. Reconciliation History-Failure Log-Forced abort, Need to add more detail why it was forced abort&#x20;

30\. Recon Dashboard History> Search field-Placeholder should be added specifying search is applicable for only application name, ako No data found should be shown and pagination should get clear when page in&#x20;

31\. Workflow-User data is not showing when user is deleted in User Details in pending workflow&#x20;

32\. Campaign Notification - In Create campaign template body showing incorrect spelling of started, it is showing "stared"&#x20;

33\. Advanced Analytics-Cron expression not updated as per user configured



#### Known Bugs

&#x20;1\. manager notification: receiving user name required manager name&#x20;

2\. In application setting if show to user flag off then also application show in recent application.&#x20;

3\. Unable to identify application properties data type where value is empty&#x20;

4\. Amaya || Create user operation fails due to invalid password&#x20;

5\. Reports-Records are getting displayed after 11min approximately&#x20;

6\. Deprovision Rule executed via Scheduler-Workflow is not getting initiated for the set of users on the basis of status/end date&#x20;

7\. Workflow List-Getting error Contact system administrator on technova tenant&#x20;

8\. FormLogic || Step 2 of form || Form config JSON should be mandatory



